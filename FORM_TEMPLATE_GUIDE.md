# คู่มือการใช้งาน Form Template

## โครงสร้างพื้นฐาน

```json
{
  "title": "ชื่อฟอร์ม",
  "description": "คำอธิบายฟอร์ม",
  "questions": [...]
}
```

## ประเภทของ Input Fields

### 1. Text Field
```json
{
  "id": "unique_id",
  "type": "text",
  "label": "ป้ายกำกับ",
  "description": "คำอธิบายเพิ่มเติม",
  "required": true,
  "placeholder": "ข้อความตัวอย่าง"
}
```

### 2. Email Field
```json
{
  "id": "email_field",
  "type": "email",
  "label": "อีเมล",
  "description": "มี validation รูปแบบอีเมลอัตโนมัติ",
  "required": true,
  "placeholder": "you@example.com"
}
```

### 3. Phone Field
```json
{
  "id": "phone_field",
  "type": "phone",
  "label": "เบอร์โทรศัพท์",
  "description": "มี validation รูปแบบเบอร์โทรอัตโนมัติ",
  "required": true,
  "placeholder": "08x-xxx-xxxx"
}
```

### 4. Date Field
```json
{
  "id": "date_field",
  "type": "date",
  "label": "วันที่",
  "description": "เลือกวันที่จาก date picker",
  "required": true
}
```

### 5. Rating Field
```json
{
  "id": "rating_field",
  "type": "rating",
  "label": "คะแนน",
  "description": "ให้คะแนน 1-5 ดาว",
  "required": true
}
```

### 6. Choices Field (Radio Buttons)
```json
{
  "id": "choices_field",
  "type": "choices",
  "label": "ตัวเลือก",
  "description": "เลือกได้เพียง 1 ตัวเลือก",
  "required": true,
  "choices": ["ตัวเลือก 1", "ตัวเลือก 2", "ตัวเลือก 3"]
}
```

### 7. Dropdown Field
```json
{
  "id": "dropdown_field",
  "type": "dropdown",
  "label": "เมนูดรอปดาวน์",
  "description": "เลือกจาก dropdown menu",
  "required": true,
  "choices": ["ตัวเลือก A", "ตัวเลือก B", "ตัวเลือก C"]
}
```

### 8. Consent Checkbox Field
```json
{
  "id": "consent_field",
  "type": "consent",
  "label": "ฉันยอมรับข้อตกลงและเงื่อนไข",
  "description": "เก็บค่า true/false เท่านั้น",
  "required": true
}
```

### 9. File Upload Field
```json
{
  "id": "file_field",
  "type": "file",
  "label": "แนบไฟล์",
  "description": "อัพโหลดไฟล์",
  "required": false,
  "accept": "image/*,.pdf,.doc,.docx",
  "maxSize": 5
}
```

## Properties ที่ใช้ได้

| Property | Type | Required | Description |
|----------|------|----------|-------------|
| `id` | string | ✅ | รหัสเฉพาะของคำถาม (ต้องไม่ซ้ำกัน) |
| `type` | string | ✅ | ประเภทของ input (text, email, phone, date, rating, choices, dropdown, file, consent) |
| `label` | string | ✅ | ข้อความหัวข้อคำถาม |
| `description` | string | ❌ | คำอธิบายเพิ่มเติม |
| `required` | boolean | ✅ | บังคับกรอกหรือไม่ |
| `placeholder` | string | ❌ | ข้อความตัวอย่างใน input (เฉพาะ text, email, phone) |
| `choices` | array | ❌ | รายการตัวเลือก (เฉพาะ choices, dropdown) |
| `accept` | string | ❌ | ประเภทไฟล์ที่รับ (เฉพาะ file) |
| `maxSize` | number | ❌ | ขนาดไฟล์สูงสุด MB (เฉพาะ file) |

## ตัวอย่างการใช้งาน

### ฟอร์มลงทะเบียนง่ายๆ
```json
{
  "title": "ฟอร์มลงทะเบียนงาน",
  "description": "กรุณากรอกข้อมูลเพื่อลงทะเบียนเข้าร่วมงาน",
  "questions": [
    {
      "id": "name",
      "type": "text",
      "label": "ชื่อ-นามสกุล",
      "required": true,
      "placeholder": "กรอกชื่อ-นามสกุล"
    },
    {
      "id": "email",
      "type": "email",
      "label": "อีเมล",
      "required": true,
      "placeholder": "you@example.com"
    },
    {
      "id": "company",
      "type": "text",
      "label": "บริษัท/องค์กร",
      "required": false,
      "placeholder": "ชื่อบริษัท"
    }
  ]
}
```

### ฟอร์มแบบสำรวจความพึงพอใจ
```json
{
  "title": "แบบสำรวจความพึงพอใจ",
  "description": "ขอความคิดเห็นของท่านเพื่อปรับปรุงบริการ",
  "questions": [
    {
      "id": "satisfaction",
      "type": "rating",
      "label": "ความพึงพอใจโดยรวม",
      "description": "ให้คะแนน 1-5 ดาว",
      "required": true
    },
    {
      "id": "recommendation",
      "type": "choices",
      "label": "ท่านจะแนะนำเราให้กับผู้อื่นหรือไม่",
      "required": true,
      "choices": ["แนะนำอย่างแน่นอน", "อาจจะแนะนำ", "ไม่แน่ใจ", "คงไม่แนะนำ"]
    },
    {
      "id": "comment",
      "type": "text",
      "label": "ข้อเสนอแนะเพิ่มเติม",
      "required": false,
      "placeholder": "แสดงความคิดเห็น..."
    }
  ]
}
```

## เคล็ดลับ

1. **ID ต้องไม่ซ้ำ**: ใช้ชื่อที่อธิบายความหมาย เช่น `email`, `phone`, `birthdate`
2. **Required vs Optional**: ตั้ง `required: true` เฉพาะข้อมูลที่จำเป็นจริงๆ
3. **Placeholder**: ใช้ตัวอย่างที่ชัดเจน ช่วยให้ผู้ใช้เข้าใจรูปแบบที่ต้องกรอก
4. **Choices**: จัดลำดับตัวเลือกที่สำคัญไว้ข้างบน
5. **File Upload**: ระบุ `accept` และ `maxSize` ที่เหมาะสม

## File Types ที่รองรับ (accept)

- **รูปภาพ**: `image/*` หรือ `image/jpeg,image/png,image/gif`
- **PDF**: `.pdf`
- **Word**: `.doc,.docx`
- **Excel**: `.xls,.xlsx`
- **ทั้งหมด**: `*/*` (ไม่แนะนำ)

## ขนาดไฟล์แนะนำ (maxSize)

- **รูปภาพทั่วไป**: 2-5 MB
- **เอกสาร**: 5-10 MB
- **ไฟล์ขนาดใหญ่**: 10-50 MB

---

## การใช้งานกับ LLM (Large Language Model)

### วิธีการสร้างฟอร์มด้วย LLM

คุณสามารถใช้ Template นี้เป็น Context ให้กับ AI เพื่อสร้างฟอร์มที่ต้องการได้อย่างรวดเร็ว

### 🎯 รูปแบบ Prompt พื้นฐาน

```
ใช้ form template นี้เป็นตัวอย่าง:

[วาง form-template.json ทั้งหมด]

ช่วยสร้างฟอร์ม [ชื่อฟอร์มที่ต้องการ] ให้ฉันหน่อย โดยมีฟิลด์ต่อไปนี้:
- [รายการฟิลด์ที่ต้องการ]
```

### 📝 ตัวอย่าง Prompt แบบต่างๆ

#### 1. Prompt แบบง่าย
```
ใช้ form template ด้านล่างนี้เป็นตัวอย่าง:

[วาง form-template.json]

สร้างฟอร์มลงทะเบียนเข้าร่วมสัมมนาให้หน่อย ต้องการฟิลด์:
- ชื่อ-นามสกุล (required)
- อีเมล (required)
- เบอร์โทร (required)
- บริษัท/องค์กร (optional)
- ตำแหน่งงาน (optional)
```

#### 2. Prompt แบบละเอียด
```
ฉันต้องการสร้างฟอร์มตามโครงสร้าง JSON ด้านล่างนี้:

[วาง form-template.json]

กรุณาสร้างฟอร์ม "แบบสอบถามความพึงพอใจในการใช้บริการ" โดยมีข้อกำหนดดังนี้:

1. ข้อมูลผู้ตอบแบบสอบถาม:
   - ชื่อ-นามสกุล (text, required)
   - อีเมล (email, required)
   - อายุ (dropdown: 18-25, 26-35, 36-45, 46-60, 60+, required)

2. การประเมินความพึงพอใจ:
   - ความพึงพอใจในคุณภาพสินค้า (rating, required)
   - ความพึงพอใจในการบริการ (rating, required)
   - ความคุ้มค่าของราคา (rating, required)

3. ข้อมูลเพิ่มเติม:
   - จะใช้บริการต่อหรือไม่ (choices: "ใช้อย่างแน่นอน", "อาจจะใช้", "ไม่แน่ใจ", "ไม่ใช้", required)
   - ข้อเสนอแนะ (text, optional)
   - แนบไฟล์รูปภาพหรือเอกสารประกอบ (file, optional, รับไฟล์รูปภาพและ PDF, ไม่เกิน 5MB)

กรุณาสร้างตาม template ที่ให้มา พร้อม placeholder และ description ที่เหมาะสม
```

#### 3. Prompt แบบแปลงจากข้อความ
```
Template reference:
[วาง form-template.json]

แปลงข้อความนี้ให้เป็น JSON form ตาม template ด้านบน:

"ฟอร์มสมัครงาน - กรุณากรอกข้อมูลดังต่อไปนี้: ชื่อ-นามสกุล, อีเมล, เบอร์ติดต่อ, 
ตำแหน่งที่สนใจสมัคร (เลือกจาก: Developer, Designer, Marketing), ระดับประสบการณ์ 
(Fresher/Junior/Mid-level/Senior), เงินเดือนที่คาดหวัง, วันที่สามารถเริ่มงานได้, 
แนบ Resume (PDF เท่านั้น ไม่เกิน 10MB), และถามว่าได้รับข้อมูลจากช่องทางไหน"
```

#### 4. Prompt แบบปรับแต่งฟอร์มที่มีอยู่
```
ฉันมีฟอร์มนี้:
[วาง JSON ฟอร์มปัจจุบัน]

Template reference:
[วาง form-template.json]

ช่วยเพิ่มฟิลด์เหล่านี้ให้หน่อย:
- วันเกิด (date, required)
- เพศ (dropdown: ชาย/หญิง/ไม่ระบุ, required)
- ภูมิลำเนินาเดิม (dropdown: กรุงเทพฯ/ภาคเหนือ/ภาคอีสาน/ภาคใต้/ภาคกลาง, optional)
```

### 💡 เคล็ดลับการเขียน Prompt

1. **ให้ Template เป็น Reference**: วาง `form-template.json` ไว้ในข้อความเสมอ
2. **ระบุรายละเอียดชัดเจน**: บอกว่าฟิลด์ไหน required/optional
3. **ระบุ Input Type**: บอกว่าต้องการ dropdown, choices, rating ฯลฯ
4. **ระบุ Choices**: ถ้าเป็น dropdown หรือ choices ให้บอกตัวเลือกที่ต้องการ
5. **ระบุ Placeholder**: อาจจะบอก placeholder ที่ต้องการด้วย
6. **ระบุ File Type**: ถ้ามี file upload ให้บอกประเภทไฟล์และขนาดที่รับ

### 🚀 ตัวอย่างการใช้งานกับ AI Models

#### ChatGPT / Claude / Gemini
```
System: คุณเป็น Assistant ที่ช่วยสร้าง Form Configuration ในรูปแบบ JSON

User: [วาง Template + คำอธิบายฟอร์มที่ต้องการ]
```

#### API Call (OpenAI)
```json
{
  "model": "gpt-4",
  "messages": [
    {
      "role": "system",
      "content": "คุณเป็น Assistant ที่ช่วยสร้าง Form Configuration ในรูปแบบ JSON ตาม template ที่กำหนด"
    },
    {
      "role": "user",
      "content": "Template:\n[form-template.json]\n\nสร้างฟอร์ม[ชื่อฟอร์ม]..."
    }
  ]
}
```

### ⚠️ ข้อควรระวัง

- ตรวจสอบ JSON ที่ LLM สร้างให้ว่า syntax ถูกต้อง
- ตรวจสอบว่า `id` ไม่ซ้ำกัน
- ตรวจสอบว่า `type` ตรงกับที่ระบบรองรับ
- ตรวจสอบว่า `choices` มีครบถ้วนสำหรับ dropdown และ choices
- ตรวจสอบว่า `required` และ `placeholder` ตรงตามที่ต้องการ

### 📋 Checklist หลังจากให้ LLM สร้างฟอร์ม

- [ ] JSON syntax ถูกต้อง (ลอง validate ด้วย JSON validator)
- [ ] `id` ไม่ซ้ำกันทั้งหมด
- [ ] `type` ถูกต้องและระบบรองรับ
- [ ] Fields ที่ควร required ถูกตั้งค่า `required: true`
- [ ] Dropdown/Choices มีตัวเลือกครบถ้วน
- [ ] Placeholder และ Description มีความหมายชัดเจน
- [ ] File upload มีการระบุ `accept` และ `maxSize`
